/// <reference types="vite/client" />

/**
 * Injected by `define` in vite.config.ts — the build this bundle was compiled
 * from. Declared here because it exists only after Vite's substitution, so
 * TypeScript has no other way to know about it.
 */
declare const __BUILD_ID__: string
